const FormOrder = () => {
  return (
    <form className="order__form">
      <input type="text" className="order__inp" placeholder="Promocode" />
      <button className="order__btn btn">Apply</button>
    </form>
  );
};

export default FormOrder;
